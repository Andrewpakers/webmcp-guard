{
  "name": "@webmcp-guard/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared types, constants, and zod schemas (policy, wire contract, audit log) for WebMCP Guard.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./storage-contract": {
      "types": "./src/storage-contract.ts",
      "default": "./src/storage-contract.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run --root ../.. --project @webmcp-guard/shared",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}

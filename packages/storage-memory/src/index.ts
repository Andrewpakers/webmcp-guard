/**
 * `@webmcp-guard/storage-memory` — in-memory `GuardStorage` implementation.
 *
 * Used by unit tests and demos, and doubles as the reference example of the
 * "bring your own database" adapter contract.
 *
 * Implemented in Phase 2 (see `docs/07-development-plan.md`). This module is a
 * placeholder so the package, its tsconfig, and the test runner are wired up.
 */
export const PACKAGE_NAME = "@webmcp-guard/storage-memory" as const;
